import chai = require('chai');

import Serialize from '../../src/serialize';
import Serializer from '../../src/serializer';

const { expect } = chai;

describe('default serializer for serializable object properties (nested serializables)', () => {

});
